const initialstate = {
    notes: [],
    active: null
};

export const notesReducer = ( state = initialstate, action ) => {

    switch ( action.type ) {
    
        default:
            return state;
    }

};